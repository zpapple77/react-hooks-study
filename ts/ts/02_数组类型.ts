{
    //数组类型：方式一(推荐)
    let numList:number[] = [1,2,3,4]
    let names:string[] = ['asc','abc']

    //方式二：泛型
    const scores:Array<number> = [100,200,300]
    const flags:Array<boolean> = [true,false]
}